using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OtherCoponents : MonoBehaviour
{
    //refering to other scripts
    /*public GameObject otherGameObject;
    private OtherCoponentsOne anotherScript;
    private OtherCoponentTwo yetAnotherScript;
    private BoxCollider boxCol;
    void Awake()
    {
        anotherScript = GetComponent<OtherCoponentsOne>();
        yetAnotherScript = otherGameObject.GetComponent<OtherCoponentTwo>();
        boxCol = otherGameObject.GetComponent<BoxCollider>();
    }
    void Start()
    {
        boxCol.size = new Vector3(3, 3, 3);
        Debug.Log("The player's score is " + anotherScript.playerScore);
        Debug.Log("The player has died " + yetAnotherScript.numberOfPlayerDeaths + " times");
    }
    Error on .Log*/
}
