using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Debug : MonoBehaviour
{
    void Start()
    {
        /*
         * don't know why its giving me errors, and only on the .Log, .LogWarning, and .LogError
        //message
        Debug.Log("This is a log message.");
        //warning
        Debug.LogWarning("This is a warning message!");
        //error
        Debug.LogError("This is an error message!");
        //warning for object
        Debug.LogWarning("I come in peace!", this.gameObject);
        //using debug on an object
        float height = transform.position.y;
        Debug.DrawLine(transform.position, transform.position - Vector3.up * height, Color.magenta, 4);*/
    }
}
